# Migration Guide

## Version Zero

There are no published stable migrations yet. Blu is still in its version-zero phase and the guidance for this line is:

- Prefer fresh scaffolds from `blu new` for new applications.
- Regenerate route and registry types with `blu types` after schema changes.
- Treat pre-1.0 package updates as potentially breaking until formal release notes exist.

